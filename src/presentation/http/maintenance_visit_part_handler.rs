//! MaintenanceVisitPart REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use rust_decimal::Decimal;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{MaintenanceVisitPartService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateMaintenanceVisitPartDto, UpdateMaintenanceVisitPartDto, PatchMaintenanceVisitPartDto, MaintenanceVisitPartResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum MaintenanceVisitPartError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for MaintenanceVisitPartError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for MaintenanceVisitPartError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "MAINTENANCEVISITPART_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "MAINTENANCEVISITPART_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "MAINTENANCEVISITPART_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "MAINTENANCEVISITPART_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for MaintenanceVisitPart.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /maintenance_visit_parts | List with pagination |
/// | POST | /maintenance_visit_parts | Create new |
/// | GET | /maintenance_visit_parts/:id | Get by ID |
/// | PUT | /maintenance_visit_parts/:id | Full update |
/// | PATCH | /maintenance_visit_parts/:id | Partial update |
/// | DELETE | /maintenance_visit_parts/:id | Soft delete |
/// | POST | /maintenance_visit_parts/bulk | Bulk create |
/// | POST | /maintenance_visit_parts/upsert | Upsert |
/// | GET | /maintenance_visit_parts/trash | List deleted |
/// | POST | /maintenance_visit_parts/:id/restore | Restore |
/// | DELETE | /maintenance_visit_parts/empty | Empty trash |
/// | GET | /maintenance_visit_parts/:id/deleted | Get deleted by ID |
/// | DELETE | /maintenance_visit_parts/trash/:id | Permanent delete from trash |
/// | GET | /maintenance_visit_parts/count | Count active entities |
/// | GET | /maintenance_visit_parts/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(MaintenanceVisitPartService::with_repository(repository));
/// let router = create_maintenance_visit_part_routes(service);
/// ```
pub fn create_maintenance_visit_part_routes(service: Arc<MaintenanceVisitPartService>) -> Router {
    BackboneCrudHandler::<MaintenanceVisitPartService, MaintenanceVisitPart, CreateMaintenanceVisitPartDto, UpdateMaintenanceVisitPartDto, MaintenanceVisitPartResponseDto>::routes(
        service,
        "/maintenance_visit_parts",
    )
}

/// Create Axum router with only the read (GET) endpoints for MaintenanceVisitPart.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_maintenance_visit_part_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_maintenance_visit_part_read_routes(service: Arc<MaintenanceVisitPartService>) -> Router {
    BackboneCrudHandler::<MaintenanceVisitPartService, MaintenanceVisitPart, CreateMaintenanceVisitPartDto, UpdateMaintenanceVisitPartDto, MaintenanceVisitPartResponseDto>::read_routes(
        service,
        "/maintenance_visit_parts",
    )
}

/// Create Axum router with only the write (mutation) endpoints for MaintenanceVisitPart.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
pub fn create_maintenance_visit_part_write_routes(service: Arc<MaintenanceVisitPartService>) -> Router {
    BackboneCrudHandler::<MaintenanceVisitPartService, MaintenanceVisitPart, CreateMaintenanceVisitPartDto, UpdateMaintenanceVisitPartDto, MaintenanceVisitPartResponseDto>::write_routes(
        service,
        "/maintenance_visit_parts",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_maintenance_visit_part_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<MaintenanceVisitPartService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_maintenance_visit_part_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

